============
Installation
============

At the command line::

    $ easy_install pulsar_telescope

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pulsar_telescope
    $ pip install pulsar_telescope